<Window x:Class="bazy1.Views.Admin.AdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:bazy1.Views.Admin"
        xmlns:viewModels="clr-namespace:bazy1.ViewModels.Admin"
        xmlns:pagesViewModels="clr-namespace:bazy1.ViewModels.Admin.Pages"
        xmlns:pagesViewModelsDoctor="clr-namespace:bazy1.ViewModels.Doctor.Pages"
        xmlns:pagesViews="clr-namespace:bazy1.Views.Admin.Pages"
        xmlns:pagesViewsDoctor="clr-namespace:bazy1.Views.Doctor.Pages"
        mc:Ignorable="d"
            xmlns:md="https://github.com/fantasticfiasco/mvvm-dialogs"

        md:DialogServiceViews.IsRegistered="True"
        Title="AdminView" Height="450" Width="1200">
    <Window.DataContext>
        <!-- Ustawia MainViewModel jako viewmodel dla tego widoku-->
        <viewModels:AdminViewModel/>
    </Window.DataContext>

    
    
    <Window.Resources>
        <DataTemplate DataType="{x:Type pagesViewModels:AddUserViewModel}">
            <pagesViews:UserAddControl/>
        </DataTemplate>
        
        <DataTemplate DataType="{x:Type pagesViewModels:ListUserViewModel}">
            <pagesViews:UserListControl/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type pagesViewModels:UpdateScheduleViewModel}">
            <pagesViews:UpdateScheduleControl/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type pagesViewModels:ModifyUserViewModel}">
            <pagesViews:UserModifyControl/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type pagesViewModels:WorkhoursViewModel}">
            <pagesViews:WorkhoursView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type pagesViewModelsDoctor:PatientListViewModel}">
            <pagesViewsDoctor:PatientListControl/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type pagesViewModels:AdminPatientListViewModel}">
            <pagesViews:AdminPatientListControl/>
        </DataTemplate>


    </Window.Resources>
    
    

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical">
            <Button x:Name="addUserButton" Width="150" Height="40" Content="Dodaj użytkownika" Command="{Binding ShowAddUserViewCommand}"/>
            <Button x:Name="listUserButton" Width="150" Height="40" Content="Lista użytkowników" Command="{Binding ShowUserListViewCommand}">
            </Button>
            <Button x:Name="Nowyharmonogram" Width="150" Height="40" Content="Nowy Harmonogramm" Command="{Binding ShowAddWorkhoursCommand}">
            </Button>
            <Button x:Name="Zmienharmonogram" Width="150" Height="40" Content="Zmień harmonogram" Command="{Binding ShowUpdateScheduleCommand}"/>

            <Button x:Name="patientList" Width="150" Height="40" Content="Lista pacjentów" Command="{Binding ShowAdminPatientListViewCommand}"/>


        </StackPanel>

        <ContentControl Content="{Binding CurrentViewModel}" Grid.Column="2"/>
    </Grid>
</Window>
