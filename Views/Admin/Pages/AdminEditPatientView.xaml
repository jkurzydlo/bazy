<UserControl x:Class="bazy1.Views.Admin.Pages.AdminEditPatientView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<StackPanel>
			<TextBlock Text="Edytuj Pacjenta" FontSize="20" Margin="10"/>
			<Label Content="Imię" />
			<TextBox Text="{Binding SelectedPatient.Name}" Width="200" Margin="10"/>
			<Label Content="Nazwisko" />
			<TextBox Text="{Binding SelectedPatient.Surname}" Width="200" Margin="10"/>
			<Label Content="PESEL" />
			<TextBox Text="{Binding SelectedPatient.Pesel}" Width="200" Margin="10"/>
			<Label Content="Telefon" />
			<TextBox Text="{Binding SelectedPatient.PhoneNumber}" Width="200" Margin="10"/>
			<Label Content="Email" />
			<TextBox Text="{Binding SelectedPatient.Email}" Width="200" Margin="10"/>
			<Button Content="Zapisz" Command="{Binding SavePatientCommand}" Width="100" Margin="10"/>
		</StackPanel>
	</Grid>
</UserControl>
