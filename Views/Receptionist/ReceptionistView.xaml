<Window x:Class="bazy1.Views.Receptionist.ReceptionistView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:bazy1.ViewModels"
        xmlns:pagesViewModels="clr-namespace:bazy1.ViewModels.Pages"
        xmlns:pagesViews="clr-namespace:bazy1.Views.Pages"
        mc:Ignorable="d"
        Title="ReceptionistView" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <!-- Ustawia ReceptionistViewModel jako kontekst danych dla tego widoku-->
        <viewModels:ReceptionistViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <DataTemplate DataType="{x:Type pagesViewModels:AddUserViewModel}">
            <pagesViews:AddUserView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type pagesViewModels:ListUserViewModel}">
            <pagesViews:ListUserView/>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <!-- Przyk³adowy interfejs u¿ytkownika dla recepcjonisty -->
        <StackPanel Orientation="Vertical">
            <Button Width="100" Height="20" Content="Dodaj u¿ytkownika" Command="{Binding ShowAddUserViewCommand}"/>
            <Button Width="100" Height="20" Content="Lista u¿ytkowników" Command="{Binding ShowUserListViewCommand}"/>
        </StackPanel>

        <ContentControl Content="{Binding CurrentViewModel}" Grid.Column="2"/>
    </Grid>
</Window>
